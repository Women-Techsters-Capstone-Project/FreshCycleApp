import 'package:flutter/material.dart';

class AppColors {
  // Primary Greens (Brand)
  static const Color primary = Color(0xFF2E7D32);        // Main brand green
  static const Color primaryDark = Color(0xFF1B5E20);    // Darker shade
  static const Color primaryLight = Color(0xFF4CAF50);   // Lighter shade
  static const Color primarySoft = Color(0xFF8BC34A);    // Soft accent green

  // Accent Oranges
  static const Color accent = Color(0xFFF57C00);
  static const Color accentDark = Color(0xFFE65100);

  // Neutrals
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF616161);
  static const Color border = Color(0xFFE0E0E0);
  static const Color disabled = Color(0xFFBDBDBD);

  // Error
  static const Color error = Color(0xFFD32F2F);
}
